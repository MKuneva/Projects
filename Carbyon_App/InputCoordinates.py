

def printCoord(climate_map_instance):
    # Get the coordinates from the ClimateMap instance
    coordinates = climate_map_instance.get_coordinates()
    
    # Print the list of coordinates
    print(f"List of coordinates added to the map: {coordinates}")